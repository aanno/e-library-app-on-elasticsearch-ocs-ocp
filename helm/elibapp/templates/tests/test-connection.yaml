apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elibapp.fullname" . }}-test-connection"
  labels:
    {{- include "elibapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elibapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
